# TASK-20260830-06: Last Relay 브랜드와 공개 README 정리

## 상태

- 상태: Done
- 우선순위: P1
- 담당자: Codex
- 관련 Outcome/Epic: 공개 저장소의 게임 정체성과 첫인상
- 선행 작업: TASK-20260830-05

## 문제와 기대 효과

- 현재 문제: `Signal Courier`만으로는 작품의 마지막 연결이라는 서사가 약하고, README가 제작 기술과 검증 구조를 앞세워 방문자가 게임 자체를 빠르게 이해하기 어렵다.
- 기대 효과: 저장소 방문자가 제목, 영상, 핵심 목표, 조작과 실행 방법만 읽고 게임의 정체성과 플레이 방식을 이해한다.
- 하지 않는 것: 게임 규칙 변경, 기존 제작·검증 문서 삭제, 미디어 재촬영.

## 산출물

1. 게임명 `SIGNAL COURIER: LAST RELAY`와 GitHub 저장소명 `Signal-Courier-Last-Relay`.
2. 게임 방문자 중심으로 다시 쓴 루트 README.
3. 브라우저 제목, Canvas 시작 화면, 패키지와 현재 제품 문서의 일관된 명칭.

## 완료 조건

- [x] GitHub 저장소와 `origin`이 새 이름을 사용한다.
- [x] README 첫 화면에서 게임 장르·목표·영상이 보이고 기술·운영 설명은 전면에서 제거된다.
- [x] 시작 화면과 브라우저 제목에 전체 게임명이 표시된다.
- [x] `npm run verify`, 공개 게임 client, `npm audit`, `git diff --check`가 성공한다.

## 검증 계획

- 정적: 이름 검색, README 링크, package 이름, GitHub repository metadata
- 게임: 빌드, Playwright 실제 입력·상태·스크린샷, 시작 화면 직접 검토
- 저장소: 깨끗한 작업 트리와 새 원격 URL 확인

## 결과

- 완료일: 2026-08-30
- 검증 증거: GitHub 저장소를 `ImGdevel/Signal-Courier-Last-Relay`로 변경하고 `origin` fetch/push와 기본 `main`을 확인했다. README를 123줄에서 63줄로 줄여 제목·짧은 소개·영상·핵심 루프·실행·조작·현재 빌드만 전면에 남겼다. 브라우저 제목, Canvas 시작 화면, package와 현재 제품 문서를 전체 이름으로 통일했다. 960×540 공개 client 스크린샷에서 2단 제목과 조작 안내가 겹치지 않음을 직접 확인했다. `npm run verify` 14 tests와 3구역 완주, media hash, `npm audit` 0건, `git diff --check`가 성공했다.
- 남은 위험: 루트 코드 라이선스는 아직 결정되지 않았다. README는 자유 재사용을 암시하지 않고 외부 자산 Credits만 연결한다.
- 후속 작업: 배포 정책을 정할 때 코드 라이선스와 브라우저 공개 배포 URL을 별도 작업으로 결정한다.
