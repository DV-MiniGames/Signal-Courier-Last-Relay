# TASK-20260829-02: 연결되지 않은 Codex MCP 제거

## 상태

- 상태: Done
- 우선순위: P0
- 담당자: Codex
- 관련 Outcome/Epic: 로컬 개발 도구 정상화
- 관련 이슈/PR: 없음

## 문제와 기대 효과

- 현재 문제: Codex에 등록된 MCP 중 일부가 `Not logged in` 상태이며 불필요한 연결 항목으로 남아 있다.
- 기대 효과: 연결되지 않은 MCP 항목을 제거해 활성 구성만 유지한다.
- 이번 작업에서 하지 않는 것: 새 MCP 서버 도입, 사용자 계정 생성, API 키 발급

## 결과물

- 정리된 Codex MCP 구성
- 설정 백업과 제거 검증 기록

## 완료 조건

- [x] 대상 MCP와 실패 원인이 식별됐다.
- [x] 변경 전 Codex 설정 백업이 존재한다.
- [x] `Not logged in` MCP가 모두 제거됐다.
- [x] 실제 시작 실패 MCP가 모두 제거되고 남은 사용자 구성 MCP가 정상 파싱된다.
- [x] `codex mcp list`로 최종 상태를 검증했다.

## 검증 계획

| 확인 대상 | 방법·명령 | 기대 결과 |
|---|---|---|
| 등록 상태 | `codex mcp list` | 대상 서버가 enabled로 표시됨 |
| 제거 상태 | `codex mcp list` | `Not logged in` 항목이 없음 |
| 설정 문법 | Codex CLI 설정 로드 | 파싱 오류 없이 목록 출력 |
| 도구 노출 | 새 Codex 세션에서 도구 목록 확인 | 연결된 MCP 도구가 표시됨 |

## 실행 기록

### 시도와 관찰

- `context7`, `sentry`, `supabase`가 enabled이지만 `Not logged in`으로 확인됐다.
- `openaiDeveloperDocs`는 enabled이며 별도 인증이 필요하지 않다.
- 현재 세션에는 Context7 도구가 노출되지 않는다.
- 변경 전 `C:\Users\imdls\.codex\config.toml.bak-TASK-20260829-02` 백업을 생성했다.
- `context7`, `sentry`, `supabase`를 제거했다.
- 추가 검증에서 `cua_repl`이 disabled로 확인되어 제거했다.
- 사용자 제공 재시작 로그에서 `firecrawl`, `postgres`, `blender-mcp`의 실제 시작 실패와 제거한 `sentry`, `supabase` 경고가 확인되어 작업을 다시 열었다.
- 재확인 시 기존 제거 항목이 설정 파일에 다시 존재했다. `codex mcp remove`는 성공을 출력했지만 현재 앱의 `config.toml`을 변경하지 않았다.
- 재시도 백업 `C:\Users\imdls\.codex\config.toml.bak-TASK-20260829-02-retry`를 만든 뒤 `blender-mcp`, `context7`, `firecrawl`, `postgres`, `sentry`, `supabase` 섹션을 설정 파일에서 직접 제거했다.
- 파일 검색과 `codex mcp list` 양쪽에서 6개 항목이 모두 사라진 것을 검증했다.

### 범위 또는 결정 변경

- 사용자 요청에 따라 로그인 복구에서 미연결 MCP 제거로 변경했다.

## 결과

- 완료일: 2026-08-29
- 검증 증거: `config.toml`에 대상 6개 섹션이 없고 `codex mcp list`에도 출력되지 않는다. 남은 사용자 구성 서버는 enabled 상태다.
- 남은 위험: 현재 실행 중인 Codex 세션은 시작 시 읽은 MCP 오류 메시지를 계속 표시할 수 있다. 앱 내장 `cua_repl` 비활성 항목은 설정에서 자동 생성되며 시작 실패 경고 대상은 아니다. Context7 제거로 저장소의 Context7 우선 조회 규칙은 실행할 수 없다.
- 후속 작업: Codex 앱 또는 CLI 세션을 완전히 종료 후 다시 시작해 시작 경고가 사라졌는지 확인한다.

## 학습 후보

| 관찰·교훈 | 분류 | 재사용 조건 | 근거 |
|---|---|---|---|
| MCP 등록·활성화와 실제 시작 성공, OAuth 로그인, 현재 세션 도구 노출은 서로 다른 상태다. | 작업 기록 유지 | Codex MCP 연결 장애 진단 시 | `codex mcp list`와 실제 시작 로그 비교 |
| Codex 앱 실행 중 `codex mcp remove`가 성공을 출력해도 앱 설정 파일에 반영되지 않을 수 있으므로 파일과 CLI 목록을 함께 검증해야 한다. | 스킬 후보 | 동일 환경에서 MCP 정리 시 | 본 작업 재시도 결과 |
