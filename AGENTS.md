# Hermes Game 실행 규칙

## 세션 시작

작업을 시작하기 전에 다음 순서로 확인한다.

1. `ops/CHARTER.md`에서 게임의 목표, 범위, 성공 기준을 확인한다.
2. `ops/BOARD.md`에서 현재 마일스톤과 유일한 `Doing` 작업을 확인한다.
3. `ops/METRICS.md`에서 이번 주 병목과 개선 지표를 확인한다.
4. `ops/MEMORY.md`에서 검증된 기술 제약과 게임 규칙을 확인한다.
5. 관련 `ops/tasks/`, `ops/decisions/`, `.agents/skills/` 문서만 추가로 읽는다.
6. `progress.md`에서 직전 구현, 테스트 증거, 남은 문제를 확인한다.

## 작업 선택과 실행

- 모든 실행은 하나의 `TASK-YYYYMMDD-NN` 작업과 연결한다.
- `Ready` 작업만 시작하며 실행 주체당 `Doing`은 하나로 제한한다.
- 구현 전에 결과물, 완료 조건, 검증 명령을 작업 기록에 적는다.
- 플레이 가능한 가장 작은 수직 조각으로 구현하고 변경 목적을 하나로 유지한다.
- 장르, 핵심 루프, 플랫폼, 아트 방향을 바꾸는 발견은 작업 범위를 조용히 넓히지 말고 ADR에 기록한다.

## 게임 구현 계약

- 웹 빌드는 단일 `<canvas>`를 기본 렌더링 표면으로 사용한다.
- 모든 플레이 가능 빌드는 `window.render_game_to_text()`로 현재 상태를 간결한 JSON으로 노출한다.
- 자동 검증을 위해 `window.advanceTime(ms)` 결정론적 시간 진행 훅을 유지한다.
- `f`는 전체 화면 전환, `Escape`는 전체 화면 종료에 사용한다.
- 새 조작은 시작 화면과 `docs/game/CONTROLS.md`에 함께 반영한다.
- 게임플레이 변경은 입력 → 중간 상태 → 결과의 전체 흐름을 자동 또는 재현 가능한 수동 절차로 검증한다.
- 스크린샷과 텍스트 상태가 다르면 스크린샷을 실제 결과로 보고 수정한다.

## 완료 규칙

작업은 다음 조건을 모두 만족할 때만 `Done`이다.

- 산출물이 존재하고 완료 조건이 충족되었다.
- `npm run check`와 `npm run build`가 성공한다.
- 플레이 변경은 Playwright 조작, 게임 상태 JSON, 실제 스크린샷으로 검증되었다.
- 콘솔에 새 오류가 없다.
- `progress.md`, 관련 작업 기록, 운영 보드가 갱신되었다.
- 후속 작업과 남은 위험이 기록되었다.

## Git 협업

- 브랜치는 `feat|refactor|hotfix/scope-description` 형식을 사용한다.
- `feat/*`, `refactor/*`는 `develop`, `hotfix/*`는 `main`에서 시작한다.
- 커밋과 PR에는 작업 ID, 변경 이유, 검증 증거, 되돌리기 방법을 남긴다.
- 생성물(`dist/`, `artifacts/`, `node_modules/`)은 커밋하지 않는다.

## 학습과 성장

- 실패, 사용자 교정, 비자명한 해결법은 작업 기록의 `학습 후보`에 남긴다.
- 여러 작업에 재사용할 수 있는 검증된 사실만 `ops/MEMORY.md`에 저장한다.
- 동일 절차가 두 번 이상 성공하면 `.agents/skills/<name>/SKILL.md` 승격을 검토한다.
- 주간 리뷰에서 메모리와 스킬을 `active`, `stale`, `archived`로 분류한다.
