{
  "name": "hermes-game-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "build": "npm run check && vite build",
    "test:game": "node scripts/verify-game.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.61.0",
    "typescript": "^5.9.0",
    "vite": "^8.0.0"
  }
}
